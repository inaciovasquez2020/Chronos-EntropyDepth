name: chronos-check

on:
  pull_request:

jobs:
  chronos-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Chronos kernel reference check
        run: |
          set -e
          grep -q "Chronos Kernel" README.md
          echo "chronos-check passed"

